global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'backend'
    static_configs:
      - targets: ['portfolio-backend:9000']  # your FastAPI backend

  - job_name: 'node-exporter'
    static_configs:
      - targets: ['host.docker.internal:9100']  # Node Exporter running on host
